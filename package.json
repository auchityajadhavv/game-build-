{
  "name": "prism-drift-tilt",
  "version": "1.0.0",
  "private": true,
  "description": "Prism Drift — a gyro-tilt endless drifter, packaged for Android with Capacitor.",
  "scripts": {
    "add:android": "cap add android",
    "sync": "cap sync android",
    "open:android": "cap open android",
    "build:apk": "cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
